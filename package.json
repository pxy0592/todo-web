{
  "name": "todo-web",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test:unit": "node --test tests/todo-*.test.js tests/smoke.test.js",
    "test:coverage": "node --experimental-test-coverage --test tests/todo-*.test.js tests/smoke.test.js",
    "test:e2e": "playwright test",
    "build": "node scripts/build.mjs",
    "format:check": "prettier --check .",
    "check": "npm run format:check && npm run lint && npm run test:unit && npm run build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.55.0",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "prettier": "^3.6.2"
  }
}
